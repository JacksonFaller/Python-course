FROM python:3.13-slim

WORKDIR /app

COPY pyproject.toml .
COPY exercises ./exercises

# TODO: install the project from pyproject.toml.
# TODO: expose the API port.
# TODO: choose the command that runs the Module 07 FastAPI app.
