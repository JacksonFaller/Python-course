[project]
name = "inventory-api"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "pydantic",
    "sqlalchemy",
    "psycopg[binary]",
    "uvicorn",
]

[dependency-groups]
dev = ["pytest", "httpx", "ruff"]

[tool.pytest.ini_options]
pythonpath = ["src"]
